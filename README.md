Ubuntu package needed to get `gloss` to install:

    libqt4-opengl-dev
    libghc-glut-dev

-----

Build with makefile after installing `gloss` with `cabal`. Resulting binary should display an animated plant.